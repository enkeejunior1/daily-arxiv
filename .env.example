# Optional but recommended: avoids Semantic Scholar's shared unauthenticated rate limit.
SEMANTIC_SCHOLAR_API_KEY=

# Used only by scripts/x-sync.mjs for local testing/backfill.
# For automatic collection, store this as the GitHub Actions secret X_BEARER_TOKEN.
X_BEARER_TOKEN=
